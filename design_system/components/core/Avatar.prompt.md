The author's portrait or a serif monogram with a thin gilt ring.

```jsx
<Avatar src="/me.jpg" alt="Thomas Scrace" size={48} />
<Avatar initials="TS" />
```

Falls back to serif initials on a raised surface when `src` is absent. Set `ring={false}` to drop the gilt outline.
