A small pill topic label for posts and filters. Quiet outline by default, gilt fill when `active`.

```jsx
<Tag href="/tag/essays">Essays</Tag>
<Tag active>Reading</Tag>
```

Render as a link (`href`), a button (`onClick`), or a static `span` (neither). Use for post categories and the filter row on the homepage.
