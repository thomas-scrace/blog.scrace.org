A tiny status marker for inline use beside titles or meta. Tones: `neutral`, `accent` (gilt), `warn` (terracotta), `quiet` (sage).

```jsx
<Badge tone="accent">New</Badge>
<Badge tone="warn">Draft</Badge>
```

Keep labels to one short word. Differs from `Tag`: Badge marks state, Tag classifies topic.
