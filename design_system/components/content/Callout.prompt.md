A quiet aside on a raised surface with a left gilt rule — notes, epigraphs, editorial marks within an essay.

```jsx
<Callout label="Note">
  An earlier version of this essay appeared in the notebook.
</Callout>
```

Tones: `note` (gilt), `warn` (terracotta), `quiet` (sage). Keep it short — it is an aside, not a section.
